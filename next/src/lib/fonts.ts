import { Bitter, Raleway } from 'next/font/google';

export const bitter = Bitter({
  subsets: ['latin'],
  display: 'swap',
});

export const raleway = Raleway({
  subsets: ['latin'],
  display: 'swap',
});
